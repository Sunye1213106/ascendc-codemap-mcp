
我觉得不要盲目修改了 你自己真实查一下我的数据库 结合下面这20个问题 看看接口应该怎么改 FlashAttentionScoreGrad arch35 — MCP A/B 测试问卷（20 题）

源码基线：用户提供的 mcp_test.bundle，HEAD 9c937de2a。
目标：比较同一个 Agent 在“普通源码工具”与“普通源码工具 + MCP”两种条件下的查询效率与答案质量。

A/B 使用规则

Baseline 组：允许正常源码工具（例如文件打开、局部 rg/搜索、跳转、读取），不提供 MCP / CodeMap。

MCP 组：完全相同的模型、prompt、代码、预算和普通源码工具，额外提供 MCP / CodeMap。

不要求 MCP 组“必须调用 MCP”；要测的是 Agent 是否会自然选择它，以及它是否真的减少源码探索。

两组都只基于当前 bundle 的代码，不给旧答案、旧问卷标准答案或预先摘出的 symbol 列表。

每题独立 session；两组题序最好随机化，但使用同一组题。

最终回答要求关键结论带 path:line。如果静态源码不足以证明运行时根因，应明确写“只能确认静态结构，不能据此锁定本次运行时根因”。

固定上下文

代码范围：attention/flash_attention_score_grad
架构：arch35 / DAV_3510 / RegBase
请基于当前源码回答，关键结论带 path:line。
不要只给函数名，要把问题涉及的上下游关系讲完整；静态源码不能证明的运行时结论不要说死。

Q1｜空输出为什么没进正常 tiling

我在正常 tiling 里打断点，一个 dq/dk/dv 里只要有输出 numel=0 的 case 就完全进不来。这个 case 是模板没命中，还是更早就被处理了？把 arch35 从 tiling 入口到空 tensor kernel/tiling data 的链路说清楚。

Q2｜TND 全零长度是不是也走 empty

TND 下 actual_seq_qlen/actual_seq_kvlen 全是 0，但 dq/dk/dv 的输出 shape 本身不是空的。3510 上会不会和 Q1 一样直接走 empty tensor？如果不会，源码里在哪里区分这两种情况，后续又怎么标记“有 S=0 的 TND 数据”？

Q3｜keepProb=1 但传了 dropMask 为什么失败

我们有个 case keepProb=1，但上层还是把 dropMask optional input 传进来了，tiling 直接报参数错误。这个限制是为什么？如果把 keepProb 改成 <1，除了真正启用 dropout，它还可能影响哪些 tiling / 分核条件？

Q4｜Host s1Inner=64，Kernel 模板=128 是不是不一致

日志里 Host 的 s1Inner 经常是 64，但 kernel 对应模板基本块看起来是 128。有人准备把 Host 直接改成 128，让两边“统一”。这个改法对不对？把 Host tile、Cube tile、Vector tile 的关系解释清楚；FP32 且 D>256 时再检查一下有没有特殊情况。

Q5｜coreNum=72，Kernel 为什么按 36 个 AIC 算

Host 日志里 coreNum=72，到了 kernel 又看到 AIC 数按 coreNum/2=36 使用。reviewer 怀疑 Host 把 AIV 数错写成了 coreNum。请从 平台取核数 → TilingData → SetBlockDim → kernel 还原 AIC 数 把这条链路展开，判断这个 /2 是 bug 还是设计如此。

Q6｜enablePreSfmg=1 是否保证一定跑 Presfmg

某个 BF16、D=128 的 case，Host 日志里 enablePreSfmg=1。这是不是已经足够保证 kernel 一定会跑 Presfmg？为什么 D=64 不会跑？另外再确认一下：现在打开确定性后，Presfmg 是否仍然是一律关闭？

Q7｜FP32 为什么没有 Post

同一个 shape，FP16 路径能看到 Post，FP32 只跑 Main。有人怀疑 FP32 漏了 cast 或 reduce。请按真实 kernel launch 链路确认这是不是设计行为，并说明 普通 BN2GS1S2/BN2S2 路径与 BN2 路径 在 Pre/Main/Post 组织上有什么差别。

Q8｜加 sink 后 workspace 为什么明显变大

加了 sink 输入以后 workspace 明显变大，而且后面还有 dsink 相关处理。请从 sink 输入校验 → Host tiling 参数 → workspace 分配 → kernel 写入/后处理 把 dsink 的产生链路追完整。不要只停在某一个 set_* 字段上。

Q9｜超 L2 了，为什么 D=96 还是不开 NzOut

一个 FP16、BN2GS1S2、S1/S2 都很大（例如 4096）、已经满足超 L2 条件的 case，D=96 时 NzOut 仍然没开。这个现象正常吗？如果只把 D 改成 88，Host 条件上会发生什么变化？还需要哪些条件才能真正打开？

Q10｜开 dropout 后 L1 buffer 为什么降档

同样是 FP16、D=128、BN2GS1S2，没 dropout 时 Query 的 L1 看起来会走 4-buffer；一开 dropout 就掉了一档。这个变化是运行时动态策略，还是编译期模板条件决定的？把 Q / K / Dy 在这组条件下各自的 buffer 选择说清楚。

Q11｜TND dense 确定性 swizzle 为什么有的 case 开、有的不开

最近 TND dense 确定性也支持 swizzle 了，但两个看起来差不多的 case，一个能开、一个不能开。除了“是否超 L2 / 是否值得 swizzle”这种总开关外，Host 还会做哪些 合法性检查和收益检查？哪些 shape/调度条件会直接否决？

Q12｜GQA dense 新调度 ≠ GQA 也能 swizzle？

我看到最近加了 GQA dense 的确定性调度优化。这个改动是不是意味着 g>1 的确定性 case 也能像 MHA 一样开 block swizzle？请把 GQA dense schedule 和 block-index swizzle/split 两套逻辑分开追，判断它们到底是什么关系。

Q13｜两个 RIGHT_DOWN_CAUSAL 为什么进了不同确定性路径

两个确定性 RIGHT_DOWN_CAUSAL case，其他设置基本一样，只是一个 S1==S2，另一个 S1!=S2。最后日志里的确定性类型不一样。这个差异在哪一层决定？两种 shape 分别会被映射到哪类确定性处理？

Q14｜BAND 确定性为什么会改用 causal/dense 调度

这版代码里 BAND 确定性调度不再只有一种排法。我想确认：Host 在什么情况下会把 BAND 的执行调度近似成 causal，什么情况下会选 dense，什么情况下保持 band？如果这些新调度的收益/利用率不够，最终如何回退？kernel 又从哪里拿到这个选择？

Q15｜旧结论说“TND 确定性 causal 没编”现在还成立吗

有人拿旧 review 结论说：“TND 确定性 causal 根本没编，碰到就是 561003。”请按 当前 arch35 模板全集 重新核对，不要只看模板文件第一段：TND 确定性在不同分核路径上当前有哪些组合已经编进去了？swizzle 组合也一起检查。最后把“Host 能产出”和“模板已编译”分开说。

Q16｜Host 能识别 FP8，是否代表 kernel 已经编了 FP8

Host 代码已经能识别 FP8/HIFLOAT8，也能把它们编码进 tiling key。那 arch35 当前是不是已经真正编出了这些 dtype 对应的非空 kernel？如果线上报 561003，应该怎样区分 Host 会产生这个 key 和 模板里存在对应 kernel 对象？

Q17｜RoPE D=128 为什么模板 D 变成 192

一个 RoPE case 实际 head dim 是 128，但 tiling 日志里模板 D 变成了 192，同时 D 不等标记也被打开。这个是 bug 还是有意的 key 编码？非 RoPE、真实 D=192 的 case 会不会被这个规则连带影响？请从 Host 取模板值一直追到模板合法组合。

Q18｜改 TilingData 时，Host/Kernel 最容易在哪种组合上选错 struct

最近 TND 确定性 swizzle 又多了确定性前缀/调度相关数据。我准备改 TilingData，但最担心 Host 和 kernel 选到不同布局。请按当前代码把 非 TND / TND、确定性 / 新确定性、swizzle 这些条件最终对应的 TilingData 布局列清楚，并指出新增字段时最容易改错哪几类组合。

Q19｜SyncAll 与 batch schedule 静态审查

多流场景偶发 hang，reviewer 怀疑全核同步和 batch schedule 没对上。只做静态源码审查：哪些阶段会做 all-core sync，Host 在什么条件下设置 batch schedule？代码里那个 BN2 + multi-block + TND 的 schedule 例外当前到底可不可达？请不要把静态关系直接说成“已经定位了这次 hang”。

Q20｜给 tiling key 新增一维，完整影响面有哪些

我准备给 arch35 的 tiling key 再加一个布尔维度。请按当前真实代码做一次改动影响面盘点：从 Host 产生这个值 → key 位布局/打包 → SEL 编译矩阵 → kernel 入口模板参数 → 最终消费，哪些位置必须同步修改？哪些位置是否需要改取决于这个新维度是否影响 TilingData 布局？目标是避免“Host 已经产出新 key，但压根没有对应 kernel 对象”的问题。 @ops-transformer/attention/flash_attention_score_grad/.ascendc-codemap/arch35/FlashAttentionScoreGrad.arch35.uo   给你一个参考 对，这个 `.uo` 跟我前面按仓库结构推断的情况不一样。**你索引实际上已经做得相当完整了。**我这次没有走 MCP 的 renderer，而是直接打开你上传的 `FlashAttentionScoreGrad.arch35.uo` 查 SQLite 数据。

实际里面有：

* `entity`: **34,489**
* `relation`: **144,417**
* `source_span`: **12,839**
* `source_line`: **75,151 行**
* `source_fts`: **已经存在**
* `legal_key`: **4,617**
* `legal_key_dim`: **87,723**
* TilingKey 19 个维度完整
* metadata 里甚至写着 `tiling_consumed_field_producer_coverage = 137/137`、`strict_closure_ok=true`

而且你仓库 `accel.py` 自己也明确把 `source_line` 定义成“real grep”，再在上面建 trigram FTS。

所以这次结论和之前明显不同：

> **当前主要问题不是 UO 没信息，而是 Agent-facing query 没把 UO 已经存在的信息有效暴露出来。**

我直接拿问卷试。

---

## Q10：dropout 后 L1 buffer 为什么降档

这题 **UO 已经可以完整回答，不需要外部 grep。**

`source_fts("IS_SMALL_D_PRELOAD")` 直接命中：

`op_kernel/arch35/flash_attention_score_grad_block_cube.h:149-151`

```cpp
constexpr static bool IS_SMALL_D_PRELOAD =
    IS_L1_PRELOAD && IS_L1_REUSE &&
    HEAD_DIM_ALIGN <= Aligned128 &&
    !IS_DROP &&
    SPLIT_AXIS == BN2GS1S2 &&
    DETER_SPARSE_TYPE == NO_DETER &&
    !IS_FP32_INPUT;
```

三个 selector 也全部在 UO：

`flash_attention_score_grad_block_cube.h:51-82`

```cpp
Q:
SMALL_D -> 4buff
else REUSE+PRELOAD -> 3buff

K:
SMALL_D -> 3buff
else REUSE -> DB

Dy:
SMALL_D -> 4buff
else REUSE+PRELOAD -> 3buff
```

而 FP16、D=128 下：

* `GET_IS_L1_PRELOAD`：D≤192、非 FP32/FP8，成立，`common.h:240-250`
* `GET_IS_L1_REUSE`：非 old-deter、D≤256、非 FP32/FP8，成立，`common.h:254-263`

所以：

| 条件        | Q     | K        | Dy    |
| --------- | ----- | -------- | ----- |
| 无 dropout | 4buff | 3buff    | 4buff |
| dropout   | 3buff | DB/2buff | 3buff |

**这是编译期模板条件，不是运行时动态 buffer 策略。**

这题之前 Agent MCP 查了半天再 grep，实际上 UO 自己已经有全部答案。

---

## Q5：coreNum=72，kernel 为什么 `/2=36`

这题也可以直接完整回答。

Host：

`tiling_normal_regbase.cpp:769-781`

```cpp
fBaseParams.coreNum = compileInfoPtr->aivNum;
fBaseParams.aicNum = compileInfoPtr->aicNum;
```

或：

```cpp
coreNum = ascendcPlatform.GetCoreNumAiv();
fBaseParams.coreNum = coreNum;
fBaseParams.aicNum = ascendcPlatform.GetCoreNumAic();
```

所以 **`coreNum` 明确就是 AIV 数，不是 AIC 数。**

然后：

`tiling_normal_regbase.cpp:2271`

```cpp
s1s2BNGS1S2BaseParams_->set_coreNum(fBaseParams.coreNum);
```

Kernel：

`flash_attention_score_grad_kernel_deter.h:347`

```cpp
int64_t k =
    tilingData->s1s2BNGS1S2BaseParams.coreNum / NUM_TWO;
```

所以 72 → 36 是：

> AIV 72 → CV 1:2 配比 → AIC 36

不是 Host 写错。

而 `SetBlockDim` 另外在 `tiling_normal_regbase.cpp:1919-1931` 根据 deterministic/path 计算，不应该把 `coreNum` 与最终 task block 数混为一谈。

---

# Q11：TND dense deterministic swizzle 为什么有的开、有的不开

这题 UO 也已经非常强。

Host 总入口：

`tiling_normal_regbase.cpp:824-854`

先有总开关：

```cpp
enableSwizzle =
    (isExceedL2Cache || isLargeInvalidBlk)
    && blockOuter == aicNum;
```

然后 deterministic TND 还要求：

```cpp
splitAxis == BN2GS1S2 || BN2S2
deterSparseType == DETER_DENSE
g == 1
IsTndDeterSwizzleSupported(...)
```

最后还要：

```cpp
layout == TND
b < TND_SWIZZLE_PREFIX_NUM
!isSeqExistZero
tailZeroCount == 0
```

其中 `IsTndDeterSwizzleSupported` 又拆成两个检查。

### 合法性

`tiling_normal_regbase.cpp:113-140`

每个 batch：

```cpp
actualSeqQLen > 0
actualSeqKvLen > 0

m = ceil(qLen / cubeBaseM)
n = ceil(kvLen / cubeBaseN)

m >= min(aicNum, n)
```

不满足直接拒绝。

### 收益

`tiling_normal_regbase.cpp:143-184`

计算：

* validSlotNum
* totalSlotNum
* slot utilization
* candidateMaxRound
* legacyMaxRound
* round growth

最后：

```cpp
slotUtilizationEnough &&
roundGrowthWithinLimit
```

才开。

所以这题不是一句“超 L2 就开”。你的 UO 已经能把**总开关 + legal gate + profitability gate**完整取出来。

---

# Q14：BAND 为什么会换成 causal/dense

这题更能说明你的图其实做得不错。

UO 里的 `deterBandScheduleMode` 本身已经是一个真正的跨层 contract：

```text
Host writer:
tiling_normal_regbase.cpp:2306

TILING_FIELD:
tiling_data_regbase.h:139

Kernel readers:
kernel_deter.h:493 CalBandDeterIndex
kernel_deter.h:594 CalDeterMaxLoopNum
```

这是我直接从 `relation` 表里查到的 `WRITES/READS`。

Host 策略：

`tiling_normal_regbase.cpp:247-338`

初始：

```cpp
mode = BAND
```

### 换 DENSE

`289-297`

要求：

```cpp
denseK == k
min(denseK, n) <= m
denseRound < bandRound
```

才改成 DENSE。

### 换 CAUSAL

`300-323`

要求 BAND 能安全嵌入 lower causal triangle，而且浪费足够小：

```cpp
lowerWaste <= upperWaste
lowerWaste <= (bandBlocks - 1) / 10
```

同时：

```cpp
causalPairCount > 0
causalK == k
causalK <= lowerCausalSize
causalRound < currentRound
```

才改 CAUSAL。

### 整个新策略放弃

`326-337`

如果：

```cpp
slot utilization 不够
或
round growth 超限
```

直接：

```cpp
return {};
```

也就是 `DISABLED`。

Kernel 再从 TilingData 读取：

`kernel_deter.h:511-540`

明确 dispatch：

```text
CAUSAL -> CalCausalSwizzleIndex
DENSE  -> CalDenseSwizzleIndex
BAND   -> CalBandHybridIndex
```

所以 Q14 几乎是 **UO contract 的标准展示题**。

---

# Q15：旧说法“TND deterministic causal 没编”成立吗

**不成立。**

这里甚至不需要猜源码，我直接查你的 `legal_key_dim`。

TND (`IsTnd=1`) 的 deterministic 编译矩阵：

| DeterType    |    SplitAxis | Swizzle | legal keys |
| ------------ | -----------: | ------: | ---------: |
| OLD=1        |  BN2GS1S2(0) |       0 |        256 |
| DENSE=2      |     BN2GS1S2 |       0 |        576 |
| DENSE=2      |     BN2GS1S2 |       1 |        288 |
| DENSE=2      |     BN2S2(5) |       0 |        112 |
| DENSE=2      |        BN2S2 |       1 |        112 |
| **CAUSAL=3** | **BN2GS1S2** |   **0** |    **288** |
| BAND=4       |     BN2GS1S2 |       0 |        288 |
| BAND=4       |        BN2S2 |       0 |         56 |

所以：

> **TND deterministic causal 当前明确编了 288 个合法 key。**

但 **causal + swizzle 当前没有 legal key**。

Host 也确实能够产生 `DETER_CAUSAL`：

`tiling_normal_regbase.cpp:1217-1223`

包括：

```cpp
LEFT_UP_CAUSAL
RIGHT_DOWN_CAUSAL && isS1S2Same
...
→ DETER_CAUSAL
```

TND 又有专门：

`tiling_varlen_regbase.cpp:121-139`

```cpp
CalcleTNDCausalDeterParam()
```

因此这题你的 UO 可以非常漂亮地把：

> Host 可生成

和

> 编译矩阵确实存在

分开证明。

---

# Q16：Host 能识别 FP8 ≠ kernel 已编 FP8

这一题 UO 给出的结果非常明确。

Host：

`tiling_common_regbase.cpp:1725-1735`

已经支持：

```text
FLOAT32 -> 1
BF16    -> 2
FP16    -> 3
FP8 E5M2 -> 4
FP8 E4M3 -> 5
HIFLOAT8 -> 6
```

TilingKey declaration 也声明了：

`template_tiling_key.h:57-64`

```text
InputDType:
1 FLOAT32
2 BFLOAT16
3 FLOAT16
4 FP8_E5M2
5 FP8_E4M3FN
6 HIFP8
```

但是我直接查这个 UO 的 4,616 个**非空 legal kernel keys**：

```text
InputDType=1 : 1040
InputDType=2 : 1788
InputDType=3 : 1788

InputDType=4 : 0
InputDType=5 : 0
InputDType=6 : 0
```

所以 Q16 的答案非常明确：

> Host 当前可以编码 FP8/HIFLOAT8 key，**但这个 arch35 `.uo` 的实际已编译非空 kernel matrix 里没有任何 4/5/6 key。**

561003 时这正是应该检查的区别。

这个问题是你的 UO **明显比 grep 更有优势**的题。

---

# Q17：RoPE D=128 为什么变 192

Host 直接写死了这个 policy：

`tiling_common_regbase.cpp:906-920`

```cpp
if (fBaseParams.hasRope) {
    fBaseParams.dTemplateType = NUM192;
    return NUM192;
}
```

所以真实 D=128 + RoPE → 模板 D=192 是**有意行为**。

同时：

`tiling_normal_regbase.cpp:1867-1872`

```cpp
dNoEqual =
    (d1 != d) || hasRope;
```

所以 RoPE 也必然把 `IsDNoEqual` 打开。

我再直接查 `legal_key_dim`：

```text
IsRope=1                          224 keys
IsRope=1 + DTemplateNum=192      224 keys
IsRope=1 + DTemplateNum=128        0 keys
```

也就是 **所有 RoPE legal key 都绑定 D=192**。

但：

```text
IsRope=0 + DTemplateNum=192      760 keys
```

因此真实非 RoPE D=192 并不会被误当成 RoPE。

这题 UO 也是完整闭环。

---

# Q19：BN2 + multi-block + TND schedule 例外到底可不可达

这题答案是：

> **按当前 Host 静态逻辑，这个组合不可达。**

`SetScheduleMode`：

`tiling_normal_regbase.cpp:1933-1936`

```cpp
if (splitAxis != BN2 ||
    !isBn2MultiBlk ||
    layoutType != TND) {
    SetScheduleMode(1);
}
```

唯一不设置 batch schedule 的组合就是：

```text
BN2 && isBn2MultiBlk && TND
```

但 `isBn2MultiBlk` 的产生条件：

`tiling_common_regbase.cpp:1667-1680`

明确包含：

```cpp
bnSparseLimit =
    bnLimit &&
    layoutType != INPUT_FORMAT_TND &&
    ...
```

然后：

```cpp
isBn2MultiBlk = bnSparseLimit && ...
```

所以：

```text
isBn2MultiBlk == true
⇒ layoutType != TND
```

与例外要求的：

```text
layoutType == TND
```

矛盾。

因此静态上该 exception 是死组合。

另外 UO 已经收录了 SyncAll 的实际站点，例如：

```text
pre_regbase.h:282                 SyncALLCores
presfmg_regbase.h:576             SyncALLCores
block_vec.h:963/983               DeterCompute
block_vec.h:997/1095              DeterComputeDq
block_vec.h:1106/1144             DeterComputeDkv
block_vec.h:1183/1288             DeterComputeDqkv
kernel_base.h:2395                SyncALLCores
kernel_deter.h:1084               Process_NEW_DETER
```

所以这题同样根本不需要全仓 grep。

---

# Q20：加一个 TilingKey bool 维的完整影响面

当前 UO 直接告诉我它有 **19 个维度**：

```text
IsEmptyTensor
SplitAxis
InputDType
IsTnd
IsDrop
IsPse
IsAttenMask
S1TemplateNum
S2TemplateNum
DTemplateNum
DeterType
IsNEqual
IsBn2MultiBlk
IsDNoEqual
IsRope
OutDType
IsNzOut
IsTndSwizzle
IsRegbase
```

位布局定义在：

`template_tiling_key.h:49-125`

当前已经用到 bit 54。

Host packing：

`tiling_normal_regbase.cpp:1892-1900`

19 个值按相同顺序进入：

```cpp
GET_TPL_TILING_KEY(...)
```

编译矩阵：

```text
101 个 ASCENDC_TPL_ARGS_SEL group
4617 legal keys
```

Kernel template 入口：

`entry_regbase.h:197-200`

模板参数同样按这组维度进入 `RegbaseFAG<>`。

所以新 bool 至少涉及：

```text
Host value
   ↓
ASCENDC_TPL_ARGS_DECL 位布局
   ↓
GET_TPL_TILING_KEY 参数
   ↓
101 个 SEL group 是否允许 0/1
   ↓
kernel template 参数
   ↓
真正 consumer
```

而 TilingData **不是无条件要改**。

目前 layout 类型只依赖：

`entry_regbase.h:217-219`

```cpp
FlashAttentionScoreGradTilingData...
<
    NEED_DETER(deterType),
    needDeterPrefix,
    isTnd,
    isTndSwizzle
>
```

所以新 bool：

* 只是影响 kernel 分支 → **TilingData layout 不必改**；
* 如果它决定 struct member/layout → 才必须同时修改 Host `GetTilingData<>`/kernel alias/注册布局。

---

# 这次实际 `.uo` 给我的结论已经很清楚了

我刚刚这些答案：

**没有打开 ops-transformer 源码文件，没有 native grep。**

我只查了你 `.uo` 里的：

```text
source_fts
source_line
entity
relation
legal_key
legal_key_dim
```

而上面 Q5/Q10/Q11/Q14/Q15/Q16/Q17/Q19/Q20 都已经能回答得很好。

所以我现在会明确撤回前面“先补 lexical index”“直接塞 rg”这种建议。

## 你现在真正的问题是 query surface 没把 UO 能力释放出来

当前 MCP public `codemap_query` 只暴露：

```text
resolve
contract
impact
entry
find
trace
```

没有一个明确对应你已经存在的 `source_fts` 检索面的 operation。

与此同时 `find name=` 被定义成了 **name discovery**，miss 后又自动 token recovery。

于是 Q10 实际发生的是：

```text
Agent 想搜 bufferNum
       ↓
find name=*BufferNum*
       ↓
不是去 source_fts 找包含 BufferNum 的源码
       ↓
在 entity name 里查不到
       ↓
自动降级 Buffer
       ↓
返回一堆 Buffer entity
       ↓
Agent 得不到新信息
       ↓
native grep
```

而你的 `.uo` 明明一次：

```text
source_fts("IS_SMALL_D_PRELOAD")
```

就能直接打到整个答案核心。

**这才是现在最值得修的地方。**

我建议现在先不要继续提交代码。拿这份实际 UO 和 20 道问卷，先把 **“理想查询轨迹”逐题跑出来**：每题记录“我作为 Agent 最初知道哪些词 → source_fts 能找到什么 → graph 能追加什么 → legal_key 能证明什么”。做完这 20 题以后，再决定 `find/search/resolve/contract` 的 API 边界。这样就不会再出现一天改三轮 renderer、ranking、recovery，最后实际上底层 75k 行 FTS 索引根本没被 Agent 用起来的情况。
  你先跑完这20个题目的mcp查图 你总结一下你要怎么改 怎么把uo的潜能释放到最大
